node_modules/
dist/
.DS_Store
*.log
.env

# runtime state written by the admin page / layout saves
config/modules.json
config/layouts/

# compiled per-machine, source sits next to it
modules/propresenter-timers/ltc-capture

# the launcher's build output, and its cache of the Node runtime it embeds
launcher/macos/build/
launcher/macos/.cache/
